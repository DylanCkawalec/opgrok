# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/

# ── Rust ──────────────────────────────────────────────────────────────────
/target/
Cargo.lock
**/*.rs.bk

# ── Secrets ───────────────────────────────────────────────────────────────
.env
.env.local
*.key
*.pem

# ── Editors / OS ──────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
*.bak
.vscode/
.idea/
*.swp
*.swo
.cursor/

# ── Logs / temp / DBs ─────────────────────────────────────────────────────
*.log
logs/
tmp/
temp/
*.tmp
*.db
*.sqlite
*.sqlite3
*.db-journal

# ── Node (local only) ─────────────────────────────────────────────────────
node_modules/
npm-debug.log*

# ── Build artifacts ───────────────────────────────────────────────────────
dist/
build/
*.egg-info/

# ── Per-clone factory output (not the published tree) ─────────────────────
core/binaries/

# ── Personal / not part of the public CLI ─────────────────────────────────
# Local tests, long-form notes, TLA+ specs, UI shells, and example oracles
# stay on the author's machine. A clone should learn the factory from
# README.md + core/skills/opgrok/SKILL.md.
tests/
docs/
apps/
webapp/
examples/
_analysis/
.n8n/
n8n_data/
postgres_data/

# Local Grok Build plugin + experimental workflows
.grok-plugin/
.grok/workflows/

# n8n / web UI helper scripts (need apps/)
ops/scripts/run.sh
ops/scripts/run_n8n.sh
ops/scripts/run_n8n_local.sh
ops/scripts/run_genius_mode.sh
ops/scripts/stop_n8n_local.sh
