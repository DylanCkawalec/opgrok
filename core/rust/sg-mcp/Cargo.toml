[package]
name = "opgrok-sg-mcp"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "MCP-style tool surface for the OPGROK SuperGrok catalog — list, route, describe, and load Grok agent skills as machine-readable JSON"
keywords = ["mcp", "grok", "xai", "agents", "llm"]
categories = ["api-bindings", "development-tools"]
readme = "README.md"

[[bin]]
name = "opgrok-sg-mcp"
path = "src/main.rs"

[dependencies]
opgrok-sg-runtime = { path = "../sg-runtime", version = "0.1.0" }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
clap = { workspace = true }
